import React from 'react'
import ReactDOM from 'react-dom/client'
import WexerPsi from '@/components/wexer-psi'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <WexerPsi />
  </React.StrictMode>
)
